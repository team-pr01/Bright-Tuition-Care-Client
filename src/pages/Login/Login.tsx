import { Helmet } from "react-helmet-async";

const Login = () => {
    return (
        <div>
            <Helmet>
        <title>Bright Tuition Care | Login</title>
      </Helmet>
            login
        </div>
    );
};

export default Login;